{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ErroriApiSistemaQuiz",
  "description": "Schema descrittivo degli errori nell'API SistemaQuiz",
  "type": "object",
  "properties": {
    "timestamp": {
      "description": "Il momento esatto in cui si è verificato l'errore, generato dal backend.",
      "type": "string",
      "format": "date-time",
      "examples": ["2025-05-05T21:00:00.541Z"]
    },
    "status": {
      "description": "Il codice di stato HTTP associato all'errore",
      "type": "integer",
      "examples": [404, 400]
    },
    "error": {
      "description": "La frase HTTP standard associata al codice di stato",
      "type": "string",
      "examples": ["Not Found", "Bad Request"]
    },
    "message": {
      "description": "Un messaggio che fornisce spiegazioni sui dettagli dell'errorer",
      "type": "string",
      "examples": [
        "Domanda con ID 5 non trovata.",
        "ID Domanda mancante nella richiesta.",
        "Utente '123456' non trovato."
      ]
    },
    "path": {
      "description": "Il percorso richiesta API che ha causato l'errore.",
      "type": "string",
      "examples": ["/eseguiTest"]
    },
    "details": {
      "description": "Un eventuale elenco di stringhe contenente dettagli aggiuntivi, spesso usato per errori di validazione nell'inserimento dei capi",
      "type": "array",
      "items": {
        "type": "string"
      },
      "examples": [
          ["Il campo 'codice' non puo' essere vuoto.", "L'ID domanda non puo' essere nullo"]
      ]
    }
  },
  "required": [
    "timestamp",
    "status",
    "error",
    "message",
    "path"
  ]
}